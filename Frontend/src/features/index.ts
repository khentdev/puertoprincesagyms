// re-export the routes from other features

import type { RouteRecordRaw } from "vue-router";

const featureRoutes: RouteRecordRaw[] = [
    /**
     * e.g. 
     * ...authRoutes,
     */
]
export default featureRoutes